# SO del servidor
build_environment: Ubuntu 14.04.3 LTS

# Lenguaje usado
language: python

# Version 
python:
  - "2.7.6"

# Instalar bibliotecas y modulos necesarios
install:  
  - sudo apt-get install python-dev
  - sudo easy_install Flask
  
# Ejecucion tests (integracción continua)
script:
  - python test.py
